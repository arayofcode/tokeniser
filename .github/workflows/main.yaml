name: CI

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: self-hosted
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
      
      - name: Run tests
        run: make test